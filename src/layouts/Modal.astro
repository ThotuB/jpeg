---
interface Props {
	id: string;
}

const { id } = Astro.props;
---

<div
	id={id}
	class='fixed z-50 inset-0 bg-black bg-opacity-50 items-center justify-center overflow-hidden hidden'
	onclick="this.classList.add('hidden');"
>
	<slot />
</div>
