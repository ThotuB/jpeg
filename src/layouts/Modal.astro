---
interface Props {
	id: string
}

const { id } = Astro.props
---

<div
	id={id}
	class="bg-opacity-50 fixed inset-0 z-50 hidden items-center justify-center overflow-hidden bg-black"
	onclick="this.classList.add('hidden');"
>
	<slot />
</div>
