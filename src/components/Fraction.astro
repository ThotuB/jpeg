---
interface Props {
	numerator: string
	denominator: string
}

const { numerator, denominator } = Astro.props
---

<div class="divide-secondary flex flex-col divide-y-4 px-1 text-center">
	<p>{numerator}</p>
	<p>{denominator}</p>
</div>
