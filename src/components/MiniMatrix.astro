---
interface Props {
	text: string
}

const { text } = Astro.props
---

<div
	class="border-secondary bg-primary text-secondary grid grid-flow-row grid-cols-4 gap-x-0.5 rounded-xl border-4 px-3 py-2 font-mono text-2xl font-bold"
>
	{text.split('').map((c) => <div>{c}</div>)}
</div>
