---
interface Props {
	numerator: string;
	denominator: string;
}

const { numerator, denominator } = Astro.props;
---

<div class='flex flex-col divide-y-4 divide-secondary text-center px-4'>
	<p>{numerator}</p>
	<p>{denominator}</p>
</div>
